apiVersion: v1
kind: Service
metadata:
  name: aetherv-orchestrator
  namespace: aetherv
  labels:
    app: aetherv-orchestrator
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: aetherv-orchestrator

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: aetherv

resources:
  - namespace.yaml
  - configmap.yaml
  - secret.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml

# Optional: Add common labels
commonLabels:
  app.kubernetes.io/name: aetherv-server
  app.kubernetes.io/component: server
